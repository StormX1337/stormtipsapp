auto-install-peers=true
strict-peer-dependencies=false
# React Native's Metro bundler resolves modules by walking node_modules upwards
# and does not understand pnpm's isolated symlink layout, so the workspace uses
# the hoisted linker — this is what Expo documents for pnpm monorepos.
node-linker=hoisted
shamefully-hoist=false

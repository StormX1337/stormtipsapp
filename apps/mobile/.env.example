# Expo public runtime configuration.
# Copy to `.env`. Everything here is compiled into the app bundle, so it must
# contain public values only — never a secret.
EXPO_PUBLIC_API_URL=http://localhost:4000
EXPO_PUBLIC_WS_URL=ws://localhost:4000/ws
EXPO_PUBLIC_APP_NAME="PROFIT TIPS"
